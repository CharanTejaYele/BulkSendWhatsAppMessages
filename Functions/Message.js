// Message.js

// Define the message as a constant
const getMessage = (contact) => {
  return `Hiiii`;
};

const getImage = () => {
  return `Logo.jpg`;
};

module.exports = { getMessage, getImage };
